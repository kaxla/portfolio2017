  <%= form_with model: Contact.new, url: contact_path do |f| %>
    <%= f.text_field :name, placeholder: 'Name' %>
    <%= f.email_field :email, placeholder: 'Email' %>
    <%= f.text_area :message, placeholder: 'Message' %>
    <%= f.submit 'Send', class: 'send-button' %>
  <% end %>
