<h1><%= @project.title %></h1>
<p><%= @project.description %></p>
<% if logged_in? %>
  <%= link_to "Delete Project", project_path(@project), method: :delete, data: {confirm: "No, but really?"} %>
  <%= link_to "Edit Project", edit_project_path(@project) %>
<% end %>
